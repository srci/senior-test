export class OrderDetail {
    constructor({ id, id_orden, id_parte, cantidad }) {
      this.id = id;
      this.id_orden = id_orden;
      this.id_parte = id_parte;
      this.cantidad = cantidad;
    }
  }