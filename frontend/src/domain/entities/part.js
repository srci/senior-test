export class Part {
    constructor({ id, numero_parte, descripcion, stock_actual, costo }) {
      this.id = id;
      this.numero_parte = numero_parte;
      this.descripcion = descripcion;
      this.stock_actual = stock_actual;
      this.costo = costo;
    }
  }